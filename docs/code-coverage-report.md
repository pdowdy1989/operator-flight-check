# Code Coverage Report

## Scope

This report covers the backend Maven module using JaCoCo output generated by:

```powershell
cd backend
mvn test
```

The JaCoCo Maven plugin is already configured to generate the coverage report during the test phase.

## Verified result

From `backend/target/site/jacoco/index.html` on March 26, 2026:

- Instruction coverage: `93%`
- Branch coverage: `58%`
- Lines covered: `546`
- Total lines: `576`
- Methods covered: `220`
- Classes covered: `35`

## Requirement check

- Required minimum: `70%+`
- Target goal: `80%+`
- Verified instruction coverage result: `93%`

Task 48 is satisfied and exceeds the stated target.

## Report location

- HTML report: `backend/target/site/jacoco/index.html`
- XML report: `backend/target/site/jacoco/jacoco.xml`
- CSV report: `backend/target/site/jacoco/jacoco.csv`

## Coverage notes

- Service coverage is currently very strong at `100%` instruction coverage.
- DTO coverage is currently `100%` instruction coverage.
- The largest remaining opportunity is branch coverage in entities and a few security paths.
